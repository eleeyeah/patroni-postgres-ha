apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres-ha-eks
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: patroni-postgres-ha
  destination:
    server: https://kubernetes.default.svc
    namespace: postgres-ha
  source:
    repoURL: git@github.com:eleeyeah/patroni-postgres-ha.git
    targetRevision: HEAD
    path: helm-charts/postgres-ha
    helm:
      releaseName: postgres-ha
      valueFiles:
        - values.yaml
        - values-eks.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - Validate=true
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
  # EKS: ensure EBS CSI + gp3 StorageClass (lessons/200 + eks-addons) before sync
