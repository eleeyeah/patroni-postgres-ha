apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: patroni-postgres-ha
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          # Start with local kubeadm; enable eks when lessons/200 cluster exists
          - env: local
          # - env: eks
  template:
    metadata:
      name: "patroni-pg-{{.env}}"
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: patroni-postgres-ha
      source:
        # REPLACE: see patroni-postgres-ha/argocd/REPO.md
        repoURL: git@github.com:YOUR_ORG/YOUR_REPO.git
        targetRevision: HEAD
        path: patroni-postgres-ha/envs/{{.env}}
        directory:
          recurse: true
      destination:
        server: https://kubernetes.default.svc
        namespace: argocd
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
          allowEmpty: false
        syncOptions:
          - Validate=true
          - CreateNamespace=true
          - PrunePropagationPolicy=foreground
          - PruneLast=true
